import * as TabStories from "./Tab.stories";
import { Meta, Controls, Canvas } from "@storybook/blocks";


<Meta of={TabStories} />

## Tab:

<br />

### Uso Básico:

#### O componente `TabComponent` é usado para criar um sistema de abas com conteúdo associado.


<Canvas of={TabStories.TabComponentChildren} />


<Canvas of={TabStories.TabComponentProps} />

<Controls />


### Funcionalidade:

O componente `TabComponent` oferece a seguinte funcionalidade:

- Renderiza as abas com o conteúdo associado a cada aba.
- Permite que os usuários alternem entre as abas clicando nelas.

#### O componente tab por ser algo que é utilizado em diversas telas consegue ser alterado utilizando o plugin da switch ui:

#### plugins:

```javascript
plugins: [switchUiPlugin, generated({ prefix: "ui" })],
```

#### Configurado desta forma:

```javascript
// tailwind.config.js
{
  //...
  tab: {
    border: colors.gray["500"],
    text: colors.gray["500"],
    selectedBorder: colors.primary["300"],
    selectedText: colors.primary["300"],
  },
}
```

