import * as PopoverStories from "./Popover.stories";
import { Meta, Controls, Story, Canvas, Description } from "@storybook/blocks";

<Meta of={PopoverStories} />

## Popover:

<br />

### Uso Básico:

#### O componente `Popover` é usado para criar caixas de ferramentas de informações adicionais que aparecem quando o usuário clica sobre o elemento, para seu uso temos o button que é o que tera seu click para mostrar o popover e seus childrens são os itens de dentro do popover.

<Canvas />

<Controls />
